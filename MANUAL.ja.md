# マニュアル (日本語版)

*[English version here](MANUAL.md)*

M5Stack Cardputer ADV向けRPN(逆ポーランド記法)電卓の使い方です。HP電卓と同じ
X/Y/Z/Tの4段スタックが常時画面に表示され、そこに数値を積み(push)、関数や演算子で
変形していく操作モデルです。代数式(`2*sin(pi/4)`のような通常の数式)をそのまま
入力する電卓ではありません。

## 基本の考え方: スタックとは

画面には常に4つのレジスタが上から **T, Z, Y, X** の順で表示されています。Xが一番下
(操作の起点)で、新しい数値を入力するたびに X→Y→Z→T の順に押し上げられ(古いTは
失われます)、演算するたびに逆に下へ畳み込まれます。

```
T   0
Z   0
Y   3
X   5
```

この状態で `+` を押すと Y(3) と X(5) が消費されて `8` がXに入り、Z→Y、T→Zと繰り下がります。

```
T   0
Z   0
Y   0
X   8
```

「3 + 5」を計算するには、`3 Enter 5 +` のように**数値→数値→演算子**の順で入力します。
これがRPN(逆ポーランド記法)の基本です。

## 数値の入力

| 操作 | 動作 |
|---|---|
| 数字・`.` | Xの入力欄(下段)に文字を追加 |
| `Enter` | 入力中の数値をスタックに積む(push)。X→Y→Z→Tと繰り上がる |
| 何も入力せず`Enter` | Xを複製してYに積む(電卓の`ENTER ENTER`で二乗などに使う定番テクニック) |
| `opt` + `-` (CHS) | 入力中の数値、またはXそのものの符号を反転 |
| `Backspace`(入力中) | 直前の1文字を削除。全部消すと数値入力自体をキャンセル |
| `Backspace`(入力していない時) | Xを0にクリア(CLX) |

**負の数の入力について**: 本機の `-` キーは**常に減算専用**です(下記参照)。実機の
HP電卓と同じく、負の数を入力するには先に絶対値を入力してから `opt+-` (CHS) で符号を
反転させます。

```
5 opt+- Enter   ->  -5 をスタックに積む
```

## 演算子(Enter不要、即座に適用)

| キー | 動作 |
|---|---|
| `+` | Y + X |
| `-` | Y - X |
| `*` | Y * X |
| `/` | Y / X |
| `^` | Y の X乗 (`pow`と同じ) |
| `%` | Y を X で割った剰余 (`fmod`、符号はYに従う) |
| `!` | X の階乗(Yは使わない) |

演算子は入力中の数値があれば自動的に確定してから計算します。すでにEnter済みの
X, Yに対しても、そのまま演算子を押せば計算できます(`3 Enter 5 +` でも
`3 Enter 5 Enter +` でも同じ結果になります)。

## 関数の呼び出し方(括弧は不要)

関数名は文字キーで入力し、`Enter`で確定して初めて実行されます。代数式電卓と違い、
**引数は括弧の中に書くのではなく、あらかじめスタックに積んでおきます。**

| 引数の数 | 使い方 | 例 |
|---|---|---|
| 0(新しい値を積む) | `名前` Enter | `pi` Enter → πをスタックに積む |
| 1(Xに適用) | `名前` Enter | `9 Enter sqrt` Enter → sqrt(9) = 3 |
| 2(Y,Xに適用) | `名前` Enter | `2 Enter 10 Enter pow` Enter → 2^10 = 1024 |
| 3(Z,Y,Xに適用) | `名前` Enter | `15 Enter 0 Enter 10 Enter clamp` Enter → 10 |

2引数関数は「先に積んだ方がY、後に積んだ方がX」になる点に注意してください。
`pow`は`Y^X`なので、`2 Enter 10 Enter pow`は「2の10乗」になります。

## Tabキーによる関数名補完

関数名を入力中に**Tab**キーを押すと補完されます。もう一度押すと同じ接頭辞を持つ
別の候補に切り替わります。

```
si<Tab>   ->  sin   (もう一度Tab: sinh 、さらに: sqrt 、さらに: sq 、さらに: sto( ...)
sq<Tab>   ->  sq
sa<Tab>   ->  save
```

候補の巡回順は「よく使う数学関数が先、`save`や`sleeptime`などの制御コマンドは後」
になるように並べてあります。1文字だけでは制御コマンドまで巡回するのに何度も
Tabを押す必要があるので、2〜3文字まで打ってから押すと一発で絞り込めます。

`sto`・`rcl`・`timeset`・`dateset`のように引数が必要な語は、補完すると自動的に
`(`まで付きます。それ以外の関数・定数はすべて括弧なしで補完されます。

## スタック操作キー

| キー | 動作 |
|---|---|
| `fn` + `;` | ロールアップ(R↑): T→X, X→Y, Y→Z, Z→T と全体を回転。**Tを1回でXに呼び出せる** |
| `fn` + `.` | ロールダウン(R↓): X→T, Y→X, Z→Y, T→Z と逆回転 |
| `fn` + `S` | X ⇔ Y を入れ替え |
| `fn` + `,` | 入力中のカーソルを左に移動 |
| `fn` + `/` | 入力中のカーソルを右に移動 |
| `fn` + `Backspace` | スタック(X,Y,Z,T)を全て0にクリア |

`;`キーには上向き矢印、`.`キーには下向き矢印、`,`と`/`キーにはそれぞれ左右矢印が
印刷されています。

## メモリレジスタ(STO/RCL)

スタックとは別に、0〜9番の10個の数値メモリを持っています。計算の途中結果を
退避しておきたいときに使います。

| 操作 | 動作 |
|---|---|
| `sto(n)` Enter | Xの値をレジスタnに保存(Xの値自体は変化しない) |
| `rcl(n)` Enter | レジスタnの値を新しい数値としてスタックに積む |

```
5 Enter sto(0)     ->  Xの5をレジスタ0に保存(Xは5のまま)
...(他の計算)...
rcl(0)             ->  保存しておいた5をスタックに呼び出す
```

メモリレジスタの内容も内蔵フラッシュに自動保存され、電源を切っても保持されます。

## DEG / RAD(度数法・弧度法)の切り替え

`opt` + `D` で、すべての三角関数(`sin cos tan asin acos atan atan2`)の角度の
単位を度数法・弧度法で切り替えます。現在のモードは画面左上に `[DEG]` または
`[RAD]` と表示されます。

## 本体上でのヘルプ表示

`help` と入力してEnterを押すと、画面上にキー操作・関数一覧のリファレンスが
表示されます。

- `fn` + `;` / `fn` + `.` — ページ送り
- `Enter` または `Backspace` — 電卓画面に戻る

## スタックの保存方法

**自動保存(内蔵フラッシュ)**: 計算を行うたびに、X/Y/Z/Tの4レジスタ・メモリ
レジスタ0〜9・DEG/RAD設定がESP32内蔵のフラッシュメモリ(NVS)に自動保存され、
起動時に自動的に復元されます。SDカードは不要で、電源を完全に切ってもスタックの
状態は消えません。

**手動での書き出し(microSDカード)**: `save` と入力してEnterを押します。
microSDカードが挿さっていれば、その時点のスタックのスナップショットが
`/rpn_log.txt` にテキストとして追記されます(代数式電卓の`calc_log.txt`とは
別ファイルなので、同じSDカードを両方の電卓で使い回しても上書きされません)。

```
---- save #1 (RAD) ----
T = 0
Z = 0
Y = 3
X = 8

```

`save` を実行するたびに新しいブロックが**追記**されます(上書きではありません)。
SDカードが無い、または初期化に失敗した場合は `SD ERR` と表示されますが、それ以外の
動作には影響しません。

## 時刻と日付の設定

この基板にはRTC(時計)チップが搭載されていないため、手動またはWi-Fi(後述)で
設定しない限り時計もカレンダーも存在しません。

- `timeset(H,M,S)` — 現在時刻をセット(24時間制。例: `timeset(9,30,0)`)
- `time` — 現在時刻を表示
- `dateset(Y,M,D)` — 現在の日付をセット(例: `dateset(2026,9,17)`)
- `date` — 現在の日付を`YYYY-MM-DD`形式で表示

どちらも電源が切れるたびに「未設定」状態にリセットされるため、使いたい場合は
**起動のたびに`timeset`/`dateset`をやり直してください**。

## カードを抜かずにSDカードの中身を見る

`usbdrive` と入力してEnterを押すと、USB-Cケーブル経由でmicroSDカードをそのまま
パソコンに公開します。実行すると**電卓はキー入力を受け付けなくなり**、元に戻すには
リセットまたは電源の入れ直ししかありません。

マウントに失敗する場合は、電卓をリセットしてから `usbdebug` と入力すると、記録された
生のSDエラー内容を確認できます。

## 自動スリープ

Cardputer / Cardputer ADVには本格的な電源管理チップが無いため、代わりに
**ディープスリープ**を使います。デフォルトでは**10分間**キー入力が無いとスリープします。

- `sleeptime(n)` — アイドルタイムアウトを`n`分に設定(0で無効化)
- `sleeptime`(括弧無し) — 現在の設定値を表示

本体**上面の物理ボタン「G0/BtnA」**は、通常使用中に押すと即座にスリープ、
スリープ中に押すと復帰、という2つの役割を兼ねています。スリープ直前にスタックと
設定はフラッシュへ保存され、Wi-Fi情報が保存済みであればこのボタンでの復帰時に
自動でNTP再同期も試みます(通常の電源投入時にはこれは起きません)。

## Wi-Fiで時刻と日付を設定する

- `wifi(ssid,pass)` — 資格情報をフラッシュに保存し、即座に接続と時刻・日付の
  NTP同期を試みます(JST/UTC+9固定)。SSID・パスワードにはハイフンも使えます。
- `wifi()` — 保存済みの資格情報で再接続を試みます
- `wifi`(括弧無し) — 副作用無しで保存済みのSSIDを表示

起動時に自動でWi-Fiに接続することは一切ありません(完全にオプトイン)。

## バッテリー残量と稼働時間

- `battery` — バッテリー残量(%)と電圧を表示(例: `battery: 82% (4.05V)`)。
  専用の残量計チップが無いため概算値です。
- `uptime` — 直近の起動またはスリープ復帰からの経過時間を表示(例: `2d 03:12:45`)

## 関数リファレンス

すべて「スタックから引数を取り、結果をXに置く」動作です。表の「呼び出し例」は
必要な値を積んでから関数名を打つ手順を示します。

### 三角関数(DEG/RAD設定の影響を受ける)

| 関数 | 説明 | 呼び出し例(RAD) | 結果 |
|---|---|---|---|
| `sin` | 正弦 | `pi Enter 2 Enter / sin` | `1` |
| `cos` | 余弦 | `pi Enter cos` | `-1` |
| `tan` | 正接 | `pi Enter 4 Enter / tan` | `1` |
| `asin` | 逆正弦 | `1 Enter asin` | `1.570796327` |
| `acos` | 逆余弦 | `0 Enter acos` | `1.570796327` |
| `atan` | 逆正接 | `1 Enter atan` | `0.7853981634` |
| `atan2` | Y,Xから角度を求める | `1 Enter 1 Enter atan2` | `0.7853981634` |

### 双曲線関数

| 関数 | 説明 | 呼び出し例 | 結果 |
|---|---|---|---|
| `sinh` | 双曲線正弦 | `1 Enter sinh` | `1.175201194` |
| `cosh` | 双曲線余弦 | `0 Enter cosh` | `1` |
| `tanh` | 双曲線正接 | `1 Enter tanh` | `0.7615941560` |
| `asinh` | 逆双曲線正弦 | `1 Enter asinh` | `0.8813735870` |
| `acosh` | 逆双曲線余弦(X≧1) | `1 Enter acosh` | `0` |
| `atanh` | 逆双曲線正接(-1<X<1) | `0.5 Enter atanh` | `0.5493061443` |

### べき乗・平方根・対数

| 関数 | 説明 | 呼び出し例 | 結果 |
|---|---|---|---|
| `sqrt` | 平方根 | `16 Enter sqrt` | `4` |
| `cbrt` | 立方根 | `27 Enter cbrt` | `3` |
| `inv` | 逆数(1/X) | `4 Enter inv` | `0.25` |
| `sq` | 二乗(X²) | `7 Enter sq` | `49` |
| `pow` | Yのx乗 | `2 Enter 10 Enter pow` | `1024` |
| `exp` | eのX乗 | `1 Enter exp` | `2.718281828` |
| `log` | 常用対数(底10) | `1000 Enter log` | `3` |
| `ln` | 自然対数(底e) | `2.718281828 Enter ln` | `1` |
| `log2` | 2進対数 | `1024 Enter log2` | `10` |

### 比較・範囲・整数論

| 関数 | 説明 | 呼び出し例 | 結果 |
|---|---|---|---|
| `min` | Y,Xの小さい方 | `3 Enter 7 Enter min` | `3` |
| `max` | Y,Xの大きい方 | `3 Enter 7 Enter max` | `7` |
| `clamp` | Z,Y,Xを消費し、Zの値をY(下限)〜X(上限)の範囲に収める | `15 Enter 0 Enter 10 Enter clamp` | `10` |
| `gcd` | 最大公約数 | `12 Enter 18 Enter gcd` | `6` |
| `lcm` | 最小公倍数 | `4 Enter 6 Enter lcm` | `12` |
| `mod` | 数学的な剰余(符号はX(除数)に従う。`%`とは異なる) | `7 opt+- Enter 3 Enter mod` | `2` |

### 組合せ論・乱数

| 関数 | 説明 | 呼び出し例 | 結果 |
|---|---|---|---|
| `ncr` | 組合せ(Y個からX個) | `5 Enter 2 Enter ncr` | `10` |
| `npr` | 順列(Y個からX個) | `5 Enter 2 Enter npr` | `20` |
| `rand` | 0以上1未満の乱数を積む | `rand` | 例: `0.4213` |
| `randint` | Y〜X(両端含む)の整数乱数 | `1 Enter 6 Enter randint` | サイコロ、`1`〜`6` |

### 丸め・階乗

| 関数/キー | 説明 | 呼び出し例 | 結果 |
|---|---|---|---|
| `abs` | 絶対値 | `5 opt+- Enter abs` | `5` |
| `floor` | 切り捨て | `3.7 Enter floor` | `3` |
| `ceil` | 切り上げ | `3.2 Enter ceil` | `4` |
| `round` | 四捨五入 | `3.5 Enter round` | `4` |
| `int` | 0方向への切り捨て | `3.9 Enter int` | `3` |
| `!`(キー) | 階乗 | `5 Enter !` | `120` |
| `fact`(識別子版) | 階乗(`!`キーと同じ) | `5 Enter fact` | `120` |

### メモリ・定数

| 名前 | 説明 | 呼び出し例 |
|---|---|---|
| `pi` | 円周率πを積む | `pi` |
| `e` | 自然対数の底eを積む | `e` |
| `sto(n)` | Xをレジスタnに保存 | `5 Enter sto(0)` |
| `rcl(n)` | レジスタnをスタックに積む | `rcl(0)` |

## エラー表示

計算できない操作(0除算、負数のsqrt、範囲外のacosh/atanhなど)を行うと、画面下部に
`ERR: ...`と赤字で表示されます。スタックの内容は変化しないので、正しい値を入力し
直してください。関数名を打ち間違えてEnterした場合は `ERR: unknown '...'` と
表示されます。

## なぜ X, Y, Z の次が T なのか

HP電卓の伝統的な命名です。X, Y, Zは3次元座標軸からの連想、4段目のTは
"Top of stack"(スタックの一番上)の頭文字です。2引数演算でX, Yが消費されても、
Tの値はそのまま複製されて新しいZになる、という「一番上に居座り続ける」性質にも
合った名前になっています。
