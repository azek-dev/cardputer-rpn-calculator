# 練習用の入力例

関数の種類(0引数・1引数・2引数・3引数・組合せ論・乱数・メモリ・科学的記数法)を
一通り練習できる、実用的な公式の入力例です。基本操作は [MANUAL.ja.md](MANUAL.ja.md)
を参照してください。

表記について: `→`は次のキー操作、数値・関数名の後の`Enter`は確定操作です。
演算子(`+ - * /`)はEnter不要でそのまま押します。

## ① 三平方の定理

a=3, b=4 の直角三角形の斜辺 c = √(a²+b²) — 1引数関数(`sq`,`sqrt`)+加算

```
3 Enter → sq Enter → 4 Enter → sq Enter → + → sqrt Enter
```

結果: `5`

## ② 円の面積

半径5の円の面積 A = πr² — 0引数定数(`pi`)の活用

```
5 Enter → sq Enter → pi Enter → *
```

結果: `78.53981634`

## ③ 円柱の体積

半径3, 高さ10の円柱 V = πr²h — ②の続きで複数演算を連鎖

```
3 Enter → sq Enter → pi Enter → * → 10 Enter → *
```

結果: `282.7433388`

## ④ 複利計算

元金10万円、年利5%、10年 A = P(1+r)^n — 2引数関数`pow`

```
1.05 Enter → 10 Enter → pow Enter → 100000 Enter → *
```

結果: `162889.4627`円

## ⑤ 運動エネルギー

質量2kg, 速度3m/s、KE = 0.5mv² — 小数入力の練習

```
3 Enter → sq Enter → 2 Enter → * → .5 Enter → *
```

結果: `9`(J)

## ⑥ 傾斜角度

立ち上がり3, 奥行き4の傾斜角 — 2引数関数`atan2`(`opt+D`で度数法に切替後)

```
3 Enter → 4 Enter → atan2 Enter
```

結果: `36.86989765`°(DEGモード)

## ⑦ 組合せ・順列

6個から3個選ぶ組合せ / 5人から3人を選んで並べる順列 — 組合せ論

```
6 Enter → 3 Enter → ncr Enter    ->  20
5 Enter → 3 Enter → npr Enter    ->  60
```

## ⑧ サイコロ

1〜6の乱数 — 2引数関数`randint`

```
1 Enter → 6 Enter → randint Enter
```

結果: 1〜6のいずれか

## ⑨ 気温のクランプ

実測40℃を0〜35℃の範囲に収める — 3引数関数`clamp`

```
40 Enter → 0 Enter → 35 Enter → clamp Enter
```

結果: `35`(Z=対象値, Y=下限, X=上限の順で積む点に注意)

## ⑩ 並列抵抗

R1=100Ω, R2=200Ω、R = 1/(1/R1+1/R2) — `inv`の2回使用

```
100 Enter → inv Enter → 200 Enter → inv Enter → + → inv Enter
```

結果: `66.66666667`Ω

## ⑪ pH計算

水素イオン濃度1×10⁻⁴、pH = -log₁₀[H+] — CHSをX確定後に単独適用

```
.0001 Enter → log Enter → opt+-
```

結果: `4`(Enterで確定済みのXに直接CHSを使うパターン)

## ⑫ メモリレジスタ活用

半径5の円で、半径を打ち直さずに円周と面積の両方を求める — STO/RCLの実用例

```
5 Enter → sto(0) Enter
rcl(0) Enter → 2 Enter → * → pi Enter → *     (円周 = 31.41592654)
rcl(0) Enter → sq Enter → pi Enter → *         (面積 = 78.53981634)
```

最後の状態ではX=面積、Y=円周と、両方の結果が同時にスタックに残っているのも確認できます。

## ⑬ 科学的記数法(アボガドロ数・10のべき乗)

アボガドロ数 6.022×10²³ をそのまま入力 — `e`による指数部入力

```
6 → . → 0 → 2 → 2 → e → 2 → 3 → Enter
```

結果: `6.022e23`

10⁻⁶(0.000001)を負の指数で入力する場合は、`e`の直後の`-`が指数の符号になります。

```
1 → e → - → 6 → Enter
```

結果: `1e-06`
